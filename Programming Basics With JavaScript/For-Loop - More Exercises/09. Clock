function clock () {
    for (let hour = 0; hour <= 23; hour++) {
        for (let minutes = 0; minutes <= 59; minutes++) {
            let result = "";
            result += `${hour} : ${minutes}`
            console.log(result);
        }
    }
}
