function projectCreation(input) {
    let nameOfArchitect = input[0];
    let projects = Number(input[1]);
    let hours = projects * 3
console.log(`The architect ${nameOfArchitect } will need ${hours} hours to complete ${projects} project/s.`)
}
