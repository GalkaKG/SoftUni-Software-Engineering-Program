function password (input) {
    let name = input[0];
    let pass = input[1];
    let index = 1;
    
    while (true) {
        let entryPass = input[index++];

        if (entryPass === pass) {
            console.log(`Welcome ${name}!`);
            break;
        }
    }
}
