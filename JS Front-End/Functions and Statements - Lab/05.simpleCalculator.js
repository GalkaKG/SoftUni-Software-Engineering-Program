function order(a,b,input) {
    if (input === "multiply") {
        console.log(a * b);
    } else if (input === "divide") {
        console.log(a / b);
    } else if (input === "add") {
        console.log(a + b);
    } else {
        console.log(a - b);
    }
}
